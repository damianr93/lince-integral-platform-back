// Enums moved to @lince/types — import+re-export explicitly to work with isolatedModules
import { OcrRole, DocumentType, DocumentStatus } from '@lince/types';
export { OcrRole, DocumentType, DocumentStatus };
