[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages_latest.pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["NODE_OPTIONS='--max-old-space-size=4096' pnpm build"]

[start]
cmd = "node --max-old-space-size=512 apps/api/dist/main.js"
